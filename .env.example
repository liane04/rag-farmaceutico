# Chaves de API externas
GOOGLE_API_KEY=a_tua_chave_google
ANTHROPIC_API_KEY=a_tua_chave_anthropic

# Qdrant (vector database)
QDRANT_HOST=localhost
QDRANT_PORT=6333

# Autenticacao JWT — gerar com: python3 -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=chave_secreta_longa_e_aleatoria

# Passwords das contas iniciais (obrigatorio)
SEED_ADMIN_PASSWORD=password_do_admin
SEED_FARMACEUTICO_PASSWORD=password_do_farmaceutico
